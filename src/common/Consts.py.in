__all__ = (
        'APP',
        'VERSION',
        'DATA_DIR',
        'init_locale',
        )

import gettext

APP = '@PACKAGE@'
VERSION = '@VERSION@'
DATA_DIR = '@DATADIR@/@PACKAGE@/data'

def init_locale():
    global INIT
    try:
        INIT
    except:
        gettext.install(APP, unicode = True)
        INIT = True

init_locale()
