__all__ = (
        'APP',
        'VERSION',
        'DATA_DIR',
        'InitLocale',
        )

import gettext

APP = '@PACKAGE@'
VERSION = '@VERSION@'
DATA_DIR = '@DATADIR@/@PACKAGE@/data'

def InitLocale():
    global INIT
    try:
        INIT
    except:
        gettext.install(APP, unicode = True)
        INIT = True

InitLocale()
